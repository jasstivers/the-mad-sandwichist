<div data-controller="search" class="container">
<!-- Search Bar-->
<%# <div class="search-bar">
        <i class="search-icon fa-solid fa-magnifying-glass"></i> %>
        <%#= simple_form_for :search, url: sandwiches_path, method: :get, html: { class: "d-flex", data: { search_target: "form"} } do |f| %>
          <%#= f.input :query,
                      input_html: {
                        placeholder: "Search for Sandwiches",
                        data: { action: "keyup->search#fire",
                        search_target: "input" }
                      },
                      label: false %>
        <%# end %>
      </div>


  <!-- Search box -->

  <div class="row my-4">
    <div class="col-12 d-flex justify-content-between align-items-center">
      <div class="input-group">
        <div class="input-group-prepend">
          <button class="btn btn-outline-secondary" type="button">
            <i class="fa-solid fa-magnifying-glass"></i>
          </button>
        </div>

        <input type="text" class="form-control" placeholder="Search for sandwiches" />

        <div class="input-group-append">
          <button class="btn btn-outline-secondary" type="button">
            <i class="fas fa-bars"></i>
          </button>
        </div>
      </div>
    </div>
  </div>

  <div id="search-results" data-search-target="results">
    <!-- Dynamic search results will be injected here -->
  </div>

  <!-- Initial Sandwich Cards (renders all sandwiches if no search) -->
  <div class="row">
    <%= render 'sandwiches/sandwich_cards', sandwiches: @sandwiches %>
  </div>
</div>
<%# toggle_favorite_sandwich_path %>
